# ============================================================
# LEGACY CONFIGURATION — config.yaml is the preferred method.
# See config.yaml.example for the recommended setup.
# This .env file is supported as a fallback for single-user
# setups. If both config.yaml and .env exist, config.yaml
# takes priority.
# ============================================================

# ==========================================
# GARMIN CONNECT CREDENTIALS
# ==========================================
# Used only during initial setup (npm run setup-garmin).
# After setup, authentication uses stored tokens.
GARMIN_EMAIL=your_garmin_email@example.com
GARMIN_PASSWORD=your_garmin_password

# ==========================================
# USER PROFILE
# ==========================================
USER_HEIGHT=183
USER_BIRTH_DATE=2000-06-15
USER_GENDER=male
USER_IS_ATHLETE=true

# Units: WEIGHT_UNIT=kg|lbs (display only), HEIGHT_UNIT=cm|in (for USER_HEIGHT input)
# Both default to metric if not set.
WEIGHT_UNIT=kg
HEIGHT_UNIT=cm

# ==========================================
# SCALE CONFIGURATION
# ==========================================
# BLE scale MAC address (optional — leave empty for auto-discovery).
# If set, the app connects to this specific device.
# If not set, the app scans for any recognized scale automatically.
# Run "npm run scan" to discover nearby BLE devices.
#
# RECOMMENDED: Set this to your scale's MAC address to avoid accidentally
# connecting to a neighbor's scale (BLE range can reach through walls).
# SCALE_MAC=FF:03:00:13:A1:04

# BLE noble driver override (optional).
# Default: node-ble on Linux, abandonware on Windows, stoprocent on macOS.
# NOBLE_DRIVER=abandonware   # @abandonware/noble — stable, works on all platforms
# NOBLE_DRIVER=stoprocent    # @stoprocent/noble — modern, actively maintained

# ==========================================
# EXPORTERS
# ==========================================
# Comma-separated list of export targets. Default: garmin
# Available: garmin, mqtt, webhook, influxdb, ntfy, telegram, intervals, file, strava
# EXPORTERS=garmin

# ==========================================
# MQTT CONFIGURATION (only when mqtt exporter is enabled)
# ==========================================
# MQTT_BROKER_URL=mqtt://localhost:1883
# MQTT_TOPIC=scale/body-composition
# MQTT_QOS=1
# MQTT_RETAIN=true
# MQTT_USERNAME=
# MQTT_PASSWORD=
# MQTT_CLIENT_ID=ble-scale-sync
# MQTT_HA_DISCOVERY=true
# MQTT_HA_DEVICE_NAME=BLE Scale

# ==========================================
# WEBHOOK CONFIGURATION (only when webhook exporter is enabled)
# ==========================================
# WEBHOOK_URL=https://example.com/hook
# WEBHOOK_METHOD=POST
# WEBHOOK_HEADERS=X-Api-Key: secret123, Authorization: Bearer tok
# WEBHOOK_TIMEOUT=10000

# ==========================================
# INFLUXDB CONFIGURATION (only when influxdb exporter is enabled)
# ==========================================
# INFLUXDB_URL=http://localhost:8086
# INFLUXDB_TOKEN=my-token
# INFLUXDB_ORG=my-org
# INFLUXDB_BUCKET=my-bucket
# INFLUXDB_MEASUREMENT=body_composition

# ==========================================
# NTFY CONFIGURATION (only when ntfy exporter is enabled)
# ==========================================
# NTFY_URL=https://ntfy.sh
# NTFY_TOPIC=my-scale
# NTFY_TITLE=Scale Measurement
# NTFY_PRIORITY=3
# NTFY_TOKEN=
# NTFY_USERNAME=
# NTFY_PASSWORD=

# ==========================================
# TELEGRAM CONFIGURATION (only when telegram exporter is enabled)
# ==========================================
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF
# TELEGRAM_CHAT_ID=987654321
# TELEGRAM_TITLE=Scale Measurement
# TELEGRAM_SILENT=false

# ==========================================
# INTERVALS.ICU CONFIGURATION (only when intervals exporter is enabled)
# ==========================================
# INTERVALS_ATHLETE_ID=i123456
# INTERVALS_API_KEY=

# ==========================================
# CONTINUOUS MODE
# ==========================================
# Set to true to keep scanning for the scale in a loop.
# After each reading, waits SCAN_COOLDOWN seconds before the next scan.
# Use Ctrl+C (SIGINT) for graceful shutdown.
# CONTINUOUS_MODE=false
# SCAN_COOLDOWN=30

# ==========================================
# DRY RUN
# ==========================================
# Set to true to read the scale and compute body composition
# without uploading to Garmin Connect. Useful for testing.
# DRY_RUN=true

# ==========================================
# TOKEN STORAGE
# ==========================================
# Directory to store Garmin auth tokens.
# Defaults to ~/.garmin_tokens if not set.
# TOKEN_DIR=~/.garmin_tokens
